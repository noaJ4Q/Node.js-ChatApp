export class User {
  constructor(id, name, lastName, avatar, connected) {
    this.id = id;
    this.name = name;
    this.lastName = lastName;
    this.avatar = avatar;
    this.connected = connected;
  }
}